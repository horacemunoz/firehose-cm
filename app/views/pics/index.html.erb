<% @pics.each do |pic| %>
	<div class="ice-box span3 text-center text_decoration">
		<h2>
			<u>
			Title: <%= pic.title %>
		</u>
		</h2>

		<%= image_tag pic.image %>
		<h3>
			Message: <%= pic.message %>
		</h3>
		<h3>
		Who: <%= pic.who %>
		</h3>
		<br/>
	</div>
	<% end %>	
